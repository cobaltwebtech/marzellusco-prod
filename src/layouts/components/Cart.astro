---
import CartModal from "@/functional-components/cart/CartModal";

// let cart;
// const cartId = Astro.cookies.get("cartId")?.value;

// if (cartId) {
//   cart = await getCart(cartId);
// }
---

<CartModal client:load />
